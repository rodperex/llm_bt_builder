
# --- Base dependencies (API mode and utilities) ---
PyYAML>=6.0
requests>=2.28.0
ament-index-python

# --- LangChain & RAG dependencies ---
langchain-core<1.0.0,>=0.3.70
langchain-chroma
langchain-huggingface
langchain-google-genai
langchain-ollama
langchain-anthropic<1.0.0
langchain-openai<1.0.0
chromadb
sentence-transformers

# --- Local Mode (Hugging Face) ---
# These dependencies are required only if you want to run LLM inference locally
# using Hugging Face models (i.e. without an external API).
# This mode is not currently implemented in the nodes; uncomment if you add it.
# torch>=2.0.0
# transformers>=4.30.0
# accelerate>=0.20.3
# bitsandbytes